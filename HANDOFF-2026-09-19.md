# 皇家奶茶大師 個人網站 — 交接（2026-09-19，Claude Code → ChatGPT）

這份文件給下一個接手的 AI。它說明「星空版」個人網站在 2026-09-18～19 做了什麼、哪些已完成並驗證、哪些還在進行、哪些等使用者決定。
原始的接手規則見同目錄 `HANDOFF.md`；本文件只講這兩天的增量。

## 0. 一定要遵守的規則（使用者定的，仍然有效）
- **所有開發指令只在 WSL Ubuntu 跑**（`wsl -d Ubuntu -- bash -lc '...'`）；不要在 Windows 端跑 npm／node／tsc。
- 在 `wsl -d Ubuntu -- bash -lc '...'` 一行內寫 `$VAR`、`$?`、for 迴圈、heredoc 會被 Windows Git Bash 吃掉，產生**假綠**。多步驟一律寫成 `.sh` 檔再 `bash 檔案`。
- **不要 commit、push、部署**，不要 `git reset/clean/checkout/stash`，不要改 `app/page.tsx`（正式首頁）。目前 HEAD `41e27f9`，**未提交檔案約 75 個**，全部是這幾天的工作，都要保留。
- 不新增 npm 依賴。不要把延期項目撈回來（貓娘、人物 3D 轉場、彗星入口、3D 奶茶）。
- 每個使用者要求先開一張 Ticket（`docs/work/starlit-intro-refinement/tickets/NN-*.md`）再做；小改由 Coordinator 直接做＋一位 reviewer；大改派 Developer＋Reviewer。使用者要求 subagent 用 Opus 5 high。
- 使用者說過：「有問題的話先跟我討論再做」「不要搞得太複雜」。
- **工作目錄已搬出 OneDrive**（見第 5 節）。不要再把 harness、截圖、紀錄寫進專案的 `work/`。

## 1. 專案與環境
- 路徑：Windows `C:\Users\leslie\OneDrive\文件\ChatGPT\奶茶流\personal-site`，WSL `/mnt/c/Users/leslie/OneDrive/文件/ChatGPT/奶茶流/personal-site`。
- 技術：React 19＋vinext/Vite dev server，TypeScript 測試用 `node <file>.test.ts` 直接跑（Node 24）。
- Dev server：WSL 內 `CHOKIDAR_USEPOLLING=1 CHOKIDAR_INTERVAL=500 npm run dev -- --host 0.0.0.0 --port 5173`，預覽頁 `http://localhost:5173/starlit-full-preview`（含 3D 開場）。`bash /mnt/c/starlit-work/starlit-implementation/wait-dev.sh` 會等它起來。
- 主要檔案：`components/starlit-shell.tsx`／`.css`／`.test.ts`（整個星空殼）、`lib/site-copy.ts`（中英文案）、`lib/projects.ts`（作品資料，未動）、`lib/ember-fire.ts`（燒穿的洞／節奏）、`lib/ember-fire-gl.ts`（WebGL 火著色器）、`lib/cursor-glow.ts`（鼠標聚光座標）。
- 測試入口：`bash /mnt/c/starlit-work/starlit-implementation/followup-34/run-tests.sh`（tsc＋七支測試：site-copy、starlit-intro、ember-fire、ember-fire-gl、cursor-glow、starlit-reader、starlit-shell）。**目前全綠。**
- UI harness（Playwright，走完 3D 開場後逐分頁斷言，1440×900 與 390×844）：最新一份是 `/mnt/c/starlit-work/starlit-implementation/followup-47/ui-checks.cjs`（`harness.sh` 跑），**57 條 ALL PASSED**。派生方式：從上一份 `ui-checks.cjs` 用 `patch-harness-NN.mjs` 做錨點式 cut/swap，錨點找不到就 throw。
- 紅測慣例：每票一個 `red-test.sh`，對產品碼做一個突變、期待真的 AssertionError、再 byte-for-byte 還原（sha256 比對）。

## 2. 網站現況（`/starlit-full-preview` 上看得到的）
- **開場**：3D 星塵人物＋六段詩句（未動）。
- **/關於我**：毛哥式名片版面、照片、技能兩欄、學歷交錯時間軸（彗星脊線，亮光寬度中段最寬）、獎項、「近況與目標」、捲動顯現。
- **/我的作品**：
  - 標題中文「皇家奶茶大師的／**作品集**」，英文「Royal Milktea Master's／**Project Base**」，沒有小標、沒有 lead 句、沒有底部「探索我的 GitHub」。
  - 三張卡（Market Council 金、CB 粉紅、MilkTea 紫；之後新專案自動從色池領色：金、亮紫、亮藍、亮粉紅、亮靛、亮黃）。
  - 關閉面＝「膜」：大圖示（68px）＋專案名，沒有「01／類別」標籤。
  - 點擊：**膜被火燒穿**（不規則舌狀前緣、焦黑邊、餘燼、往上竄的火舌、火星），約 3.0 秒燒完＋0.8 秒淡出；露出開啟面：小圖示＋名稱（置中、17px）、簡介（置中、15px）、連結列 GitHub／YouTube **一左一右**（只有一個時靠左）。可多張同開、再點收回（1.4 秒）、點空白處收回、連結不收回。
  - 火是 **WebGL 著色器（方案 C，v5.1）**，琥珀核心 0.35 混進最熱兩階，其餘用各卡專屬色；WebGL 不可用時自動退回 Canvas 2D 版（v4）。
  - 燒開時**流光框 5px**（專屬色四階 conic，7 秒轉），燒穿期間**火沿四邊往外舔**（Ticket 47，剛交付、審查中）。
  - 靜止時：**鼠標聚光**（毛哥 elvismao.com 做法）——鼠標在格子區時三張卡的 1px 邊框用各自專屬色亮起、鼠標所在卡內部有淡聚光；燒開後聚光讓位給流光框。
- **三個分頁共用**：內容欄背景一層很淡的紫／金聚光跟著鼠標（含滾輪捲動時重算）；觸控裝置不掛。
- **星塵流背景已整層移除**（使用者放棄）。
- **/聯絡資訊**：未動。

## 3. 票的狀態（本輪）
| 票 | 內容 | 狀態 |
|---|---|---|
| 29–38 | About 版面／字級／時間軸／脊線亮光／詩句標點／dash／揭露修正 | 已結（更早） |
| 39 | 作品卡 v3 薄膜燒穿（CSS 版） | 已結，被 41／45 取代 |
| 40 | 作品頁零碎修整（開啟面頭列、連結左右、拿標籤、拿 GitHub 連結、標題改字、拿光跡） | **PASS，結票** |
| 41 | 火 v4（Canvas 2D） | **PASS，結票**；作為 WebGL 的退路保留 |
| 42 | 星塵 v2 | 已實作並 PASS，**但使用者隨後放棄星塵，被 43 移除** |
| 43 | 整層移除星塵 | **PASS，結票** |
| 44 | 鼠標聚光（內容欄）＋卡片邊框聚光 | **PASS，結票**（含滾輪修正） |
| 45 | 火 v5／v5.1（WebGL） | **使用者定案「火就用這版」，結票**。尾段收火（v5.2）使用者未採用 |
| 46 | 中文標題「皇家奶茶大師的／作品集」＋開啟面字級置中 | **PASS，結票** |
| 47 | 流光框 5px＋燒穿期間舔邊火 | **Developer 交付，Reviewer 審查中**（見第 4 節） |

各票檔案：`docs/work/starlit-intro-refinement/tickets/`。每票的 Developer／Reviewer 紀錄與證據：`C:\starlit-work\starlit-implementation\followup-NN\`（`developer.md`、`review/review.md`、`evidence/`）。

## 4. 還沒做完／等使用者決定
1. **Ticket 47 review 尚未回來**。Developer 自報：框 5px 四邊實量正確、舔邊火厚度上 16／左右 9–11／下 9–10px、卡面與鄰卡 0 像素、3.8 秒歸零、每幀 avg ≤0.56ms、harness 57 條全過。待 reviewer 的審美判定（像不像火、跟膜燒穿是不是同一團火、390 觀感）。Developer 未決兩點：①收回時完全不畫舔邊火（照票面字面）；②第一版像「發光金屬框」，噪音調細後才有火舌，兩版畫面在 `followup-47/evidence/look/`。
2. **舔邊火時機**：使用者只答了「粗度 5px」，沒答「燒開後一直舔」還是「只在燒穿期間舔」。目前照 Coordinator 建議做「只在燒穿 3 秒＋淡出期間」。使用者要改就改。
3. **火的最終觀感**：使用者定案 v5.1。Reviewer 的判定是「金卡與粉卡已像燒紙，紫卡在線上，1.3 秒後尾段三張卡都掉回線下」。若使用者之後嫌尾段，做法是「火舌高度與 seedBand 隨 fade 一起收」（v5.2，已寫在 45 票，未做）。
4. **使用者抱怨「火焰特效吃網路」**：已解釋——WebGL 火沒有任何網路請求；看到的流量是（a）3D 開場素材約 80 MB（`public/fantasy/source.glb` 60 MB＋星點資料）走 WSL 虛擬網卡，工作管理員算成網路；（b）OneDrive 同步尾巴。可優化的是 3D 素材（glb 壓縮、資料合併），**尚未開票**，使用者沒說要做。
5. **OneDrive**：使用者要「不准傳」。已把 `work/` 底下 192 GB（含 3D 模型試作 `model-trial` 188 GB）搬到 `C:\starlit-work\`；OneDrive 的「選擇資料夾」設定 AI 不能代點，且取消勾選會刪本機檔。給了三個選項（什麼都不做／整個專案搬出 OneDrive／關掉 OneDrive 程式），使用者未選。
6. **`components/starlit-shell.tsx` 行尾**在 9/19 中途被某位 Developer 從 CRLF 改成 LF（其他檔本來就是 LF）。功能無影響；將來 commit 時 diff 會整檔變動。使用者要改回再改。
7. **未驗證項**（各票都明寫）：真機 GPU 成本與 60fps（headless 是軟體光柵）、Safari／WebKit 的 `clip-path: url()` 與 WebGL 混合、真實 WebGL context lost、`preserveDrawingBuffer` 正式版關閉的路徑（`EMBER_GL_CAPTURE_FLAG`，只有 harness 打開）。
8. **正式首頁 `app/page.tsx` 仍是舊版**，星空版只在 `/starlit-full-preview`。何時切換、要不要 checkpoint commit，都要使用者說。

## 5. 工作目錄搬家（2026-09-19，重要）
- 專案在 OneDrive 內，`work/` 下的 harness 證據與 3D 試作共 192 GB 被持續上傳，吃滿網路。已把 `work/*` 全部子資料夾搬到 **`C:\starlit-work\`**（WSL `/mnt/c/starlit-work/`），名稱不變：`starlit-implementation`（所有 followup-NN）、`model-trial`、`portrait-orbit-run`、`first-pose-model`、`image-intro-preview`、`camera-model`、`camera-analysis`、`image-orbit-review`、`mao-reference`、`python`、`spiral-intro-v22-before`、`before-arc-pan-…`、`rejected-arc-pan-…`。
- 專案內 `work/` 只剩 `README-MOVED.md` 與兩個舊 `.cjs`。產品碼沒有引用這些資料夾。
- 舊腳本內寫死的 OneDrive 絕對路徑是歷史紀錄；`followup-47` 起的腳本用新路徑。`starlit-shell.test.ts` 的 esbuild 產物已改寫到 `node_modules/.cache/`。
- **之後任何 harness、截圖、紀錄一律寫到 `C:\starlit-work\starlit-implementation\followup-NN\`。**

## 6. 已知的坑（省你時間）
- harness 在驗作品頁前會**先切到英文**，所以作品頁 h1 的 harness 釘子是英文字；中文標題由 `starlit-shell.test.ts` 的 SSR 釘子守。
- 全站 `html { scroll-behavior: smooth }`：headless 裡 `window.scrollTo` 幾乎不動，harness 要用 `behavior: 'instant'`。
- 一張 canvas 給過 WebGL context 就永遠拿不到 2D context；退路必須換新元素。
- 動畫量測用「暫停 `getAnimations()` 設 currentTime」；有限動畫用 `finish()`，無限動畫才 `play()`。
- 多個 Developer 同時在同一棵樹上跑 harness 會互相假紅（同一個 5173）；一次只跑一份。
- 火的顆粒／電子感量法：`x mod 4` 四堆比值（抓 2px 格子）＋「孤立亮點／千像素」（抓椒鹽），兩條都在 45 的 harness。

## 更新（2026-09-19 稍晚）
- Ticket 47 Review：**FAIL**（三條 Major：舔邊火在內容欄寬度動畫期間會畫到卡面文字上；harness 門檻被放寬蓋掉真紅；舔邊火像發光畫框不像火）。已派 Developer 第二輪修，內容見 47 票檔末段。**在 47 第二輪 PASS 之前，localhost 上的舔邊火不是定案版。**
