> 2026-09-19 使用者回覆目前好非常多，僅兩點修正：原燒穿特效結束後外框由細到粗顯現，最後維持3.5px與明暗流動；CB維持原平台名稱與定位，只圖標改調色盤，預覽不得改稱CB調色盤。背景與圖標造型方向獲正向確認，待這兩點修正後整合。

> 2026-09-19 背景最新澄清（覆蓋前次誤解）：原背景上本來就有很暗、隱約可見的雲與星星，提供空間／遠近層次。雲留在背景既定位置；滑鼠／觸碰光照過時，只將該處既有雲照成淡金色，移開恢復暗雲。不可把雲綁在滑鼠上移動或平時完全移除，也不鋪明顯亮紫雲幕。

> 2026-09-19 最新外框修訂：使用者撤回所有外火舌，回到原本流動外框；同一作品色系使用暗、亮、暗、亮四段交替，任一時刻兩側較暗、兩側較亮並持續繞框流動，提升明暗對比讓流動可辨。不要求Google四色色相；保留作品原配色與燒穿開合。

> 2026-09-19 最新使用者修正：保留原本背景，只增加滑鼠／觸碰照過時局部浮出的淡金色雲霧，附近少量星星；撤回鋪滿內容區、平時清楚可辨的紫靛星雲要求。不重做原有背景。

# 核准架構與資料流

> 2026-09-19 首版視覺否決後修訂：外火減量、沿既有框線同方向流動，火尖一律向上；消除燒穿完成後額外晚亮。背景需看見雲霧紋理與層次，不能只有星星。CB從蕾瓦娜絲頭像改成藍色調色盤；雪絨更可愛且保留角色特徵。這些是使用者明確修訂，其餘範圍不變。

> 2026-09-19 實作中使用者修訂：LinkedIn改為移除網站入口，撤回可達性查驗／修復；保留其他社群及Email，歷史證據保留。以 `docs/work/starlit-effects-portfolio/spec.md` 修訂後R5／AC6及Ticket05為準。其餘本輪核准範圍不變。

## 2026-09-19 外火、互動背景與五件作品：本輪核准架構

使用者以「核准」確認本節方向。配合 requirements.md 同日最新需求，優先於歷史衝突條款。尚未開始產品實作，不宣稱動態樣片或視覺已通過。

- Code Root：沿用 `C:/Users/leslie/OneDrive/文件/ChatGPT/奶茶流/personal-site` 的 app、components、lib、public、docs 與 React／Vite／TypeScript／既有繪圖機制，不重排整站。
- Data Root：正式圖標／必要靜態素材由 public 隨網站供應；原始參考圖留原位置且不覆寫。沿用 `C:/starlit-work/starlit-implementation/` 保存實作候選及驗證證據，不新增業務資料庫；規格及Tickets固定在docs/work。
- Runtime Root：沿用 Node、node_modules 及既有框架執行環境、建置快取，不建新服務或大型工具環境。
- 正式 Log：不需要。沿用已核准的純前端範圍，無新增背景排程、無人看守服務、Web API或多人共用資料；Console與臨時除錯即可。
- 目錄與命名：本輪独立工作包為 `docs/work/starlit-effects-portfolio/`，唯一Spec為其中spec.md，Tickets在同目錄tickets。歷史starlit-intro-refinement規格保留，不再為本輪維護第二份Spec。產品模組沿用現有責任，英文命名；不複製另一套網站。
- 資料與所有權：lib/projects維持唯一作品資料來源，雙語描述同一條目管理；新增兩件後舊首頁及星空預覽都讀五件，不分岔兩套資料。各作品id對應圖標與配色；社群沿用共用來源。
- 資料流：作品／語言資料進入StarlitShell呈現；既有開合狀態決定外火啟停；內容區滑鼠或觸碰位置喚醒背景金光及星芒；離開後按已核准餘光時間淡去。輸入只觀察正常操作，不攔截捲動或連結。
- 模組責任：外火與互動背景各自管理，接入既有卡片及內容區；StarlitShell保留頁面與卡片狀態所有權。人物、開場場景與相機不承擔新增背景特效責任。圖標以網站靜態資產或既有圖標呈現接縫承接，保持單色一致性與角色辨識度。
- 介面與Schema：沿用作品id、雙語欄位、連結清單及既有開合狀態，不新增後端或API。外火只讀取卡片是否完全展開／收回；背景只讀取內容區座標、操作與可見性，不建立第二份導覽狀態。技術實作不在未看樣片前鎖定為特定CSS技巧或shader。
- 生命週期：切離相應頁面、瀏覽器分頁隱藏時暫停特效；卸載取消動畫與監聽、釋放所持資源；返回不得重複啟動。觸控不干擾捲動與點擊。既有減少動態設定及錯誤退路須查驗，不以失效或退路掩蓋一般模式下不合格的火舌。
- 整合與依賴：沿用現有網站與主預覽入口，不新增後端、資料庫、正式Logger或無關依賴。LinkedIn在既有社群接縫查驗，分辨本地連結錯誤與外站限制；不猜測替代個人檔案。
- 相容與遷移：保留未提交內容、來源素材、既有開場／人物、3.5px框與16／14px字級；共用資料讓舊首頁增加作品是已核准的預期影響，但不替換其版面、不部署。新外火取代已被否決的外觀，不能疊加兩套持續特效。按本輪實際增量保存回復證據，不整庫reset或覆蓋他人變更。
- 測試接縫：沿用純行為測試、TypeScript與現有建置檢查，驗證狀態時機、停止／釋放、座標與餘光、資料與連結；以實際瀏覽器驗證桌面／手機及雙語。來源字串／CSS值斷言不能取代動態視覺驗收，不為通過而放寬無關斷言。
- 驗證邊界：先提供外火動態、背景互動、圖標供使用者看，再完成五張卡整合；最後走主預覽及舊首頁資料冒煙驗證。未取得視覺確認要明確標示待確認。LinkedIn以實際開啟結果回報，外部阻擋不當作修正成功；真機效能沒量不宣稱通過。
- ADR：無；沿用主體架構，沒有新批准的高成本不可逆技術選型。
- 未決事項：無待選架構方向；外火／圖標實際品質與LinkedIn根因屬實作驗證事項。繪圖技術由實作在既有接縫内選擇並以樣片證實，不能更改已核准觀感。

---

## 2026-09-16 完整視覺與角色互動：最新核准架構

本節架構方向已獲使用者核准，配合 requirements.md 最新2026-09-16需求。衝突的舊五幕、自動轉幕、圖片粒子互插及音訊接線不再是本次實作依據。架構核准不代表視覺或骨架可行性已驗證。

- Code Root：既有 personal-site，沿用 app/、components/、lib/、public/、docs/ 與 React／Vite／WebGL，不大搬家或重做整站。
- Data Root：沿用 work/ 保存原始候選及驗證產物、output/imagegen/ 保存生成圖；交付使用的必要整理素材進 public/，主預覽不可依赖另開的模型服務。
- Runtime Root：沿用現有 Node.js、node_modules、WSL與既有工具；不另建大型服務或自行安裝新建模工具。既有VPoser可用性不等於骨架生成能力，不作完成假設。
- 正式 Log：不需要，沿用之前已核准的無Worker／排程／無人看守服務／新增API或共用資料需求；Console及臨時除錯足夠。
- 目錄與命名：沿用既有模組接縫，新增路徑英文；本次唯一規格仍為 docs/work/starlit-intro-refinement/spec.md，後續Tickets在同目錄。候選不覆蓋來源；正式接入不得拷貝第二套網站。
- 資料與所有權：集中雙語內容仍持有文案；閱讀控制持有當前段、逐句進度與完成狀態；場景協調器持有當前／目標姿態、最後待處理目標、鏡頭、首入口轉場與播放生命週期。貓娘控制持有完整互動、道具歸位及30秒待機循環。
- 資料流：輸入→閱讀或章節狀態→統一場景時間及鏡頭→人物／星空／道具／貓娘渲染與文字回報。獨立自轉或待機在同一生命週期下運行，不再各開互不相知的鏡頭／播放器。
- 模組責任：StarlitShell呈現文案與控制；場景協調器承接既有命令回報；相機保留原圖終點与CCW路徑；人物動畫管理骨架、六向動作和手持物；貓娘管理分層插畫與道具接觸；彗星只服務首次內容入口。
- 貓娘：採分層2D插畫，保留原圖臉與服装；補畫獨立頭耳尾、手臂／手掌、腿與必要遮擋／姿態素材。R從品牌中分離為可動對象；皇冠、杯以可追蹤接觸位置和貓娘手勢對位。拿取／放回保持連續，前後遮擋合理，不能以整圖晃動交付。
- 人物：三姿態基礎模型整理至一致坐標、骨架及道具定位語意，先驗證一條完整轉場能保持原圖終點，再擴展六條直接有向轉場。兩個使用者GLB是圖二／三來源，圖一沿用既有模型與原圖基準。星點隨身體變形，不只是整模型剛體自轉；不硬倒播、不用整幕白光／彗星掩蓋不連續。
- 骨架與載入：優先沿用已在模型試作使用的成熟GLB讀取及動畫功能（Three.js GLTFLoader／AnimationMixer）作接入依據，避免另寫GLB格式解析；保持同一場景／鏡頭，不直接嵌入另一個獨立viewer。實作前核對套件版本及既有WebGL接縫。三模型一致骨架與中間姿態尚未實證，須先做實際動作驗證；若必須犧牲外觀或改变需求，帶證據回報，不降低標準。
- 閱讀：逐句完成狀態與整段完成門檻獨立管理，通知鏡頭可前進；不再以整塊淡入或任意固定延遲冒充毛哥效果。開場貓娘可隨時離開，後續文字不可提前向下跳。
- 轉場：首次首頁→內容為彗星光尾遮幕；內容六向為人物／背景／手持物／鏡頭連續動畫；兩者不串成雙重過場。轉場期間只更新最後待處理目標，不中斷當前動作、不堆疊佇列。
- 介面與 Schema：沿用 pose、replay、advance、readingStep、language、onIntroState 的命令／回報接縫，補齊逐句／整段完成、轉場中／落定與待處理目標的明確語意。具體型別在同一接縫擴充，不保留第二套進度真相。素材記錄來源、姿態、坐標與道具接點、動畫名稱及必要版本。
- 整合與依賴：以 /starlit-full-preview 為唯一使用者驗收入口；正式資產自同網站供應，字體與參考效果按實際毛哥網站校準，原網站品牌與文案不照搬。
- 相容與遷移：保留原始模型、歷史候選、未提交資料及正式首頁；不發布。新人物接入後才停用主預覽舊圖二／三粒子路徑，舊實驗不清除。保留雙語、重播、社群、切背景暫停與3D錯誤重試。手機不改原圖觀看角度，調版面及取景，不顯露缺腳。
- 測試接縫：沿用node:assert純行為測試及現有瀏覽器驗證；測顯字門檻、反向閱讀、連點最後目標、重播、循環與道具歸位、背景暫停、錯誤重試。模型靜態與轉場中間幀、貓娘完整動作皆目視驗證，不用來源字串存在或骨數代替行為證據。
- 驗證邊界：先一條完整人物動作驗證最難接縫，再完成所有六條和集成；候選階段不可結案。桌面／手機原圖終點對照、六向完整播放、中間畫格、完整貓娘及30秒待機、顯字翻字、彗星、奶茶全部細節、主要UI流程均須驗證，記錄效能環境而非宣稱未測FPS。
- ADR：無，保留既有主體架構；未另核准新增ADR。
- 未決事項：無待選的架構方向；三模型骨架與動作銜接的實際品質屬實作先行驗證點，不是假定已通過。

### 使用者指定派工
- 3D人物、圖畫與視覺素材相關 Developer：Astra 6 / medium（工具標識 gpt-6-astra）。
- 純程式 Developer：Opus 5 / medium。
- 兩位隔離 Reviewer（Spec、Standards）：均 Opus 5 / medium。
- 混合工作按責任拆分，不把整合程式與美術資產的所有权混在同一票；不得因CLI預設改模型。配置不可用時明示，不悄悄換模型。

---


## 2026-09-14 網站精修：核准架構

以下項目及完整架構摘要已由使用者明確核准，尚未開始實作。舊架構保留作歷史依據，不代表當前首頁實作。新需求取代舊有固定三秒、暫用文案與純文字降級要求；人物與既有動畫沿用使用者目前認可的成果，不重新開啟歷史人物重建工作。

- 正式 Log：不需要。使用者確認無背景 Worker／排程、無長時間無人看守程式、無新增 Web API 或多人共用資料功能。沿用 Console 及必要臨時除錯資訊。
- 整合方式：先在現有專案內提供獨立預覽入口，沿用目前人物與動畫成果；使用者確認畫面及聲音後才整合首頁，不自行發布。
- 同步責任：同一套開場進度統一控制鏡頭、文字顯示、閱讀停留及轉場音效；文字完成後點擊提前接續時共同轉幕。奶茶自轉及星光閃爍持續獨立運作。
- 偏好保存：中英文及靜音選擇保存在目前瀏覽器，重訪及重播保留，不跨裝置同步。初次 CLICK 啟動聲音；已靜音時 CLICK 只啟動動畫，不覆蓋靜音偏好。
- 3D 錯誤：不新增純文字、靜態或簡化版本；必要 3D 啟動失敗顯示簡短說明及重試按鈕，不將失敗當作開場完成而顯示純文字主頁。
- 音訊錯誤：音樂／音效失敗不阻擋完整動畫，聲音按鈕提供重試。
- 內容與素材：同一前端專案內集中管理中英文內容；音樂、音效與圖標隨網站載入，記錄來源與必要署名。不新增後台、資料庫或外部內容服務。更新內容與素材透過更新網站版本交付。
- 背景生命週期：離開分頁或手機切到背景時暫停開場進度與聲音，回來從原處接續，保留靜音選擇。

### 根目錄、資料流與責任

- Code Root：C:/Users/leslie/OneDrive/文件/ChatGPT/奶茶流/personal-site；沿用 React、vinext／Vite、TypeScript 及現有 WebGL2 場景，不更換引擎或重排專案。
- Data Root：沿用 work/ 容納候選素材與視聽驗證產物；正式靜態素材納入 public/。沒有資料庫或外部資料根目錄。
- Runtime Root：沿用 Node.js、node_modules 與現有框架快取／建置目錄；不另裝大型工具或另建執行環境。
- 目錄與命名：沿用 app/ 頁面、lib/ 邏輯與資料、public/ 素材、docs/ 文件的責任。新增路徑使用英文；本次唯一規格位於 docs/work/starlit-intro-refinement/spec.md。預覽是同一前端的獨立入口，共用必要模組，不複製整站或改寫舊實驗。
- 資料所有權：集中雙語內容持有五幕中文全文及對應英文、頁面內容與標籤；既有 projects 資料保持作品唯一來源。瀏覽器持有語言、靜音偏好，網站記憶體持有本次開場進度。圖標、聲音與星座資料屬網站靜態資產，保留來源與署名紀錄。
- 資料流：讀取偏好與素材 → 星空等待並顯示序詩 → CLICK 啟動開場及允許的音訊 → 統一進度回報文字與鏡頭階段 → 背面奶茶句觸發轉向獨立星座 → 正面全貌結尾 → 關於我及固定導覽。語言與點擊作為同一進度的輸入，不另外啟動平行時間軸。
- 模組責任：Home／預覽頁負責文字、控制項及章節組合；AstralScene 維持 React 與 mountFantasyScene 的橋接與啟動錯誤；場景協調器管理播放生命週期；現有 orbit／camera 承接鏡頭路徑與階段求值；renderer／legacy 保留人物與章節渲染；星座幾何及自轉與人物手持物分離；聲音依同一階段事件播放及暫停。
- 公開介面：沿用 pose、replay、advance、onIntroState 的命令與回報關係，按新流程擴充等待、五幕及錯誤語意，串接語言、啟動、可見性與聲音控制。具體型別及內部函式由實作依既有接縫調整，不能建立第二套互不相知的開場流程。
- Schema／API：只使用前端內容資料與本機偏好；不新增 Web API、登入、多人共用狀態或資料庫 Schema。逐幕計時依語言與文字長度求值，取代固定三秒；切語言保留目前幕及鏡頭位置。
- 相容與遷移：保留主頁及所有未提交／被忽略素材，先預覽後由使用者確認才整合。章節重構與既有手持物保持；新增奶茶星座不改成手持物。3D 失敗不回報成功完成；音訊失敗不阻擋視覺。不新增減少動態的簡化交付版本。
- 測試接縫：沿用 node:assert/strict 的 lib/*.test.ts、fantasy-orbit 與 fantasy-particles 等現有測試模式；驗證 CLICK 等待、五幕順序、文字完成門檻、雙語停留、提前繼續、重播、背景暫停與錯誤重試的公開行為。修訂已被新需求取代的固定三秒斷言，不能以保留舊斷言為由改回舊需求。
- 驗證邊界：型別與既有建置檢查；桌面／手機實際連續播放與關鍵中間畫格；完整五段全文與雙語覆蓋；音訊實際試聽；社群、偏好、固定導覽、重播與重試。記錄同環境修改前後效能，不以測試成功代表視覺合格。預覽視聽確認是正式整合的前置。
- ADR：無，未提出或核准難逆轉的新技術選型。
- 未決事項：無阻擋規格的架構決策。精確鏡頭參數、星點數、音量與素材、英文詩句及閱讀時長係數在製作與預覽校準，不捏造已驗證結果。

### 現況查證

- 目前 app/astral-scene.tsx 呼叫 lib/fantasy/scene.mjs 的 mountFantasyScene；不得把下方舊實驗架構直接當作目前運行路徑。
- 目前 AstralScene 發生錯誤會回報 stage=done，此行為須依本次已確認的 3D 錯誤流程調整。

---

以下為歷史人物繞拍架構，未受本次變更影響的內容保留。

## 2026-09-10 已核准模型輔助方法補充

當前隔離工作為 `work/model-trial`，使用已下載 Tripo 模型與既有本地工具。原始 GLB 與原圖不覆寫；在衍生模型修正姿勢、比例、手、臉髮與衣服，使原版首尾近乎一致。先確認實體模型的每面，再依其真實三角形 XYZ 產生固定星點；亮度影響建構時密度、暗部保有底密度，播放不換人物、不裁面硬貼二維剪影。星火綁最終真實掌心及局部座標。

部位工作可並行，但共同模型／生成資料單一整合者寫入；鏡頭與渲染單一負責者。最新可旋轉頁需支援同角度實體、材質、均勻點及最終星點對照。完整形體、原版首尾近似及動畫都通過後才整合主站。其餘原有網站架構及互動要求保持，詳見 `docs/work/portrait-orbit/spec.md` 與 `agent-brief.md`。

本次架構由使用者於 2026-09-09 核准，範圍為人物星空開場繞拍。正式 Log 已確認不需要。這是已核准的實作方向，不代表點雲形體或補圖品質已被驗證。

## 根目錄與命名

- Code Root：C:/Users/leslie/OneDrive/文件/ChatGPT/奶茶流/personal-site。沿用 React、vinext、Vite、TypeScript、自訂 WebGL 與既有 app/、lib/、public/ 結構；不搬成通用 frontend/backend 骨架。
- Data Root：沿用專案內被 Git 忽略的 work/，本次隔離實驗位於 work/image-intro-preview。補圖候選、校準與畫格檢查產物留在隔離工作區；被納入主頁的正式素材才進 public/。不建立資料庫或新的外部資料根目錄。
- Runtime Root：沿用既有 Node.js、node_modules 與框架產生的快取／建置目錄，不建立獨立大型執行環境，不增加 Blender 或其他建模工具。
- 正式 Log：不需要。本次只有瀏覽器動畫，不涉及背景 Worker／排程、長時間無人看守服務、新增 Web API 或多人共用狀態。沿用 Console；排錯需要時使用暫時 Debug Log。
- 新增資料夾及檔名使用英文；規劃、校準說明與驗收證據使用繁體中文。現有素材檔名維持原狀，不為命名整理搬動資料。

## 資料與所有權

- 原有第一姿態正面、背面頭肩與手部素材為美感及姿態基準。必要時補製少量左右側面參考，先核對同人物、髮型、衣服與伸手姿勢，再參與校準；不能因檔名較新而視為已接受。
- 校準後的同一份人物基礎 XYZ、表面顏色及表面法線由人物建構邏輯擁有。播放時保持形體與顏色對應固定；允許既有微浮動、正常視角遮擋、光照與少量亮度變化，不依鏡頭角度切換圖像或重配人物星點。
- 星空保持同一空間配置並受同一鏡頭描述；不能換一片背景假裝繞行。章節切換仍可讓背景星參與共用點池重構。
- 手持物保留自身的旋轉資料與手掌位置關係；人物不跟著物件轉。前兩鏡不可看見火種，最後全貌才出現。
- 第一輪人物候選就包含完整伸手姿態的基本形體，以便提早檢查頭肩到全貌能否連續銜接；精細校準優先處理髮束、臉、頸部、衣領、肩膀與接合。

## 資料流

1. 讀取原素材與必要的少量側面候選，檢查角色及姿態一致性。
2. 根據各方向輪廓與細節校準人物形體及色彩對應，生成固定人物星點，依修正後表面重新計算法線，供光照與可見性使用。
3. 建立同一場景的人物、星空及手持物資料；沿用現有粒子資料及角色區分方式。
4. 開場狀態機提供階段、文字進度、點擊／自動繼續狀態與鏡頭進度。
5. 相機控制由統一的角度、向外距離、注視位置與時間，求出相機位置和投影所需基底；第一段 0.9 秒，其餘校準值以核准構圖及連續性為約束。
6. WebGL 使用固定基礎人物資料與相機參數完成繞拍。基礎顏色不隨視角切圖；鏡頭運動與既有微浮動／物件旋轉分工處理。
7. 開場終點就是第一章的完整人物構圖，直接維持該場景，不切回另一份正面人物。後續章節切換才使用原有粒子重構流程。

## 模組責任

- 人物取樣／建構：沿用 portraitPoints 與隔離實驗 fixedBust 所在的取樣及形體建構接縫；保留圖片取樣能力，改善固定形體，不能把現有深度推估當作可靠側面。其他章節原有姿態維持原美感。
- 相機：沿用 bustCamera 的相機位置與基底計算接縫，統一描述兩段同方向外擴及構圖。不以 spiralTransition 的前後粒子插值作為新開場實作。
- 開場狀態：沿用 IntroSequence 與既有文案／繼續操作接縫，管理文字完成、3 秒等待、重播及階段轉換；鏡頭校準不改變互動契約。
- 渲染：AstralScene 負責資料載入、WebGL 資源、相機與粒子渲染、微浮動／閃爍、視窗變化及錯誤降級，並回報開場狀態。
- 頁面：既有頁面負責文字、品牌重播與章節選取，沿用 pose、replay、advance、onIntroState 的對外互動方式。
- 章節與物件：ParticleMorph、既有共用粒子池及手持物幾何／旋轉保持各自責任；重構可從目前可見場景接續，不得以恢復舊正面點陣掩蓋開場接合。
- 隔離驗證頁：沿用固定相機角度、統一色彩與角度對照能力，補足完整播放和時間軸畫格檢查所需的最小控制。正式網站不新增實驗控制介面。

## 介面與 Schema

- 沿用既有 Float32Array 粒子資料及 STRIDE=13 的接縫：座標、色彩、法線、角色／物件旋轉等欄位。尊重既有欄位依角色使用的語意，不能任意覆寫其他章節或物件資料。
- 相機計算沿用 eye、right、up、back 等既有資料接縫；角度、距離、注視位置與時間共同形成鏡頭結果。
- 開場保持 approach、front、turn-back、back、return、done 的既有狀態語意與頁面回報契約。return 僅為既有識別字，不代表倒轉鏡頭或回正面近景。
- 不新增 Web API、網路資料契約、登入、多人共用狀態或資料庫 Schema。
- 具體校準表與內部函式組織由實作在以上責任內決定；不得因此引入另一種動態換圖或人物重建架構。

## 整合、相容與遷移

- 先在目前隔離實驗內改善，保留主頁與所有未提交／被忽略資料。不得重置專案、覆蓋舊實驗歷史或自行發布。
- 形體與動畫通過視覺檢查後，才把必要實作及素材整合主站。正式頁面不得依賴 work/ 的檔案路徑或 3001 實驗服務。
- 開場與第一章共用連續的第一姿態場景；其他章節仍使用既有共用池重構，保留手持物與背景參與規則。
- 保留縮放與版面適應能力、減少動態偏好、素材或 WebGL 失敗時文字仍可閱讀的行為。
- 比較同一環境中修改前後的載入與播放效能，記錄實測環境及結果；不把未測量的幀率或裝置能力當作已達標事實。

## 測試接縫

- 沿用 node:assert 的 lib/*.test.ts 模式與既有純函式／狀態機測試；必要時增加能重現實際錯誤的最小檢查。
- 人物：有限座標、表面方向合理性、固定基礎資料不因相機求值而改變，以及各方向可觀察的輪廓與接合。
- 相機：兩段旋轉不反向、移動段向外、停留時穩定、構圖與終點連續；不能只驗證相機函式自身的靜態不變性就推論實際播放合格。
- 互動：文字完成前後、3 秒自動繼續、快速點擊、品牌重播、途中換章節、減少動態與錯誤情境。
- 型別與建置：沿用專案既有 tsc／build 能力。效能與視覺證據取自實際瀏覽器；不用快照或數學測試取代畫面審查。

## 驗證邊界與修正

1. 形體關：正面、左右側、背面及過渡角度，分別看單色形狀與原圖色彩。固定點數或有側面厚度不是通過依據。
2. 動畫關：完整播放及兩段逐格檢查；驗證同方向外擴、人物持續可見、沒有回近景再拉遠、火種入鏡及文字節奏。
3. 整合關：確認全貌與第一章銜接、所有章節、重播、縮放、減少動態、錯誤降級及修改前後效能。

每次視覺缺陷記錄對應角度／時間與畫格，在隔離候選修正，再複驗原缺陷及受影響播放。尚有明顯缺陷不得套入主頁或宣稱完成。若需要改動原畫、增加已核准範圍外的素材或更換路線，帶著具體證據回到使用者討論，不自行降低標準。原先失敗實驗保留，狀態說明必須分清可重現證據與歷史紀錄。

## ADR

無。本次選擇已記錄於此，尚未另行核准建立 ADR，也不以建立 ADR 作為實作前置工作。

## 未決事項

- 正式文案明確延後，沿用目前暫用文案。
- 第二段時長、精確角度／半徑／注視點、粒子密度與細部形體校準值在實作的視覺驗證中調整，必須符合已核准流程與構圖；不是重新選擇架構的許可。
- 不預設固定效能數字；使用已核准的同環境修改前後比較，記錄實測條件與限制。
- 沒有其他阻擋規格整理的未決架構事項。


## 2026-09-20 最新修訂：提前 1 秒與星雲接縫
外框開始時間改為原燒穿結束前 1000ms（約2800ms），覆蓋稍早700ms提前量；放慢的增粗時長維持700ms。背景的現有美感被使用者否決：須參考真實星雲的疏密層次、雲絲、暗塵遮蔽，使暗雲與淡金照明適合目前主題，仍固定於背景、保持文字可讀。桌面光效以右側內容區左緣為0%，到內容區寬度1/3處達100%，中間平順漸強；分界和左側人物星空完全無打光，包含受光星芒，不得以光源中心強度代替逐像素接縫衰減。手機沒有左右接縫，不套用會不必要削弱內容的左側衰減。其餘核准範圍不變。


## 2026-09-20 最新實作微調
使用者已肯定前版星雲造型，追加：背景星色沿用左側實際palette，分布金50%／淡藍25%／淡紫25%；星芒餘光2.2秒（雲1.6秒）；微動初版太少，最新雲24秒單程12px/-9px、星32秒單程6px/-4px往返。暗亮雲同相位，照明遮罩／左右接縫固定。星漂移以CSS群組處理，輸入／捲動採樣群組位置，有限餘光不追逐CSS每幀漂移而延長排程。
外框改為火／膜共用揭露輪廓第一次接觸圓角周界即出現0.5px細線，按接觸周長比例單調增粗至3.5px；不再以提前1秒／700ms增粗timer控制。GL與2D共用幾何，無每幀Reactstate或新增RAF；配色、7秒orbit、字級、開場與人物維持。
最新凍結候選01 revision9／02 revision6；美感與新動態最終確認待使用者回覆，不能把前版造型肯定當新動態接受。

最新追加：雲光淡出也與星光統一2200ms／同一曲線（02 revision7），取代上述雲1600ms；其他漂移與比例不變。


最新回覆：使用者接受01火焰外框；02雲仍不明顯，改24/-18px與12秒單程（星6/-4px32秒不變），雲星共同1500ms淡出，覆蓋上述2200ms。


## 2026-09-21 手機優化：沿用架構與已核准行為修訂
沿用現有 React／vinext、WebGL2、場景命令／回報及相機接縫，不更換引擎或新增平行場景架構。根目錄依使用者遷移指示為 D:/workstationD/personal-site；WSL /mnt/d/workstationD/personal-site。
使用者已核准手機修復方向：章節內容不可因3D延遲或失敗持續不可見。此局部錯誤行為修訂覆蓋歷史「3D失敗阻擋內容」限制，保留失敗及重試語意，不偽裝3D成功。正常桌面效果維持，手機背面相機沿用現有相機模組、參照桌面視角修正直式取景，不重定資料流或公開介面。WebGPU僅評估，不核准生產渲染引擎替換；若需改變既有架構或犧牲視覺，回報再決策。
