# Current integration preflight addendum — 2026-09-12

Read-only Coordinator preparation, NOT Ticket03 start or A13 pass. Old raw-clothes-detail-r1/main-integration-preflight.md describes R4-era data; do not execute its sample rebuilding/resampling recipe against current candidates.

Latest reviewed FULL preview FS1 contains 607702 STAR rows (600000 prefix + 7702 reference additions). Its integrated.js STAR_R1_VS uses attributes 0 position, 1 normal, 2 UV, 3 surface, 4 hand, 5 original reference RGB/role, 6 point support/size, 7 retired legacy-hand scalar. Attribute7 is per point, not mesh vertex. The legacy scalar retires duplicated old hand contours while preserving underlying hand floor. Losing this attribute recreates the user's two overlapping hands. Reference rows bypass second material/rim lighting; preserving original RGB/size and source identities matters, including role1 rows within the existing prefix. Do not assume original references only occur in appended rows.

The old suggestion to rebuild stars or uniformly spread 607702 rows down to old ~72k must not silently discard original reference features. Preserve accepted visual contracts in the isolation snapshot first; any performance adaptation needs original near/wide and whole motion comparison. GLSL300 uses uint/gl_VertexID hashing and explicit textureLod: a WebGL1 port needs explicit stable seed equivalence and texture sampling verification, not syntax-only replacement. Main already owns one shared STRIDE13 chapter pool; keep chapter continuity/held object/sky/keyboard/fallback behavior. Choose a minimal compatible data/rendering seam after final candidate passes; no renderer migration is assumed approved by this note.

FS1 pool and material bindings are on actual final triangles, normals from renderer-normalized mesh vertex normals and barycentrics. Uniform diagnostic UV retains historical R9 source-color meaning, not geometric bary-UV; do not 'repair' it by changing source coloring without evidence. Mesh need not be downloaded at runtime if finalized point/material data fully represent the approved renderer, but final storage choice must preserve appearance and be measured.

All raw assets and dependencies must become formal public/source assets; main cannot depend on HTTP3004 or ignored work/ folders. Preserve existing dirty files. Final actual main UI flow, A10 comparable performance, fallback/reduced motion, tsc/lib tests/build and independent review remain required. Current preintegration baseline checks are documented separately and do not pass final checks.
## Pending B2 hand runtime addition (not yet accepted)
B2 reports new local hand topology requires true face/bary remapping and retained source identities; old-face-index assumptions will no longer suffice. It also recovered frozen R3 attr8 physical point diameter/source once-lit material for canonical hand source rendering. Latest diagnostic source-volume01-star-physical carries this; geometrysource04-joined is later and still being transported. Do not integrate this preview blindly. Once hand accepted, preserve its verified world-diameter/source size relation and once-lit semantics alongside attr7 retired-outline data and main sharedSTRIDE13. Source original RGB/size fields remain unchanged; renderer support is distinct. Old2008 GARMENT role2 and2398 R3 PRIMARY source sets cannot be joined by ordinal; require explicit mapping/diff. Final approved snapshot must define exact schema and all-body protection before Main integration.

## OVERRIDE — 使用者改用 NEW fantasy GLB（2026-09-13）
上方FS1/B2模型與607702列/舊attr7退休欄位是已退休舊模型歷史，不得當新模型整合固定輸入或強制沿用。讀new-fantasy-model-switch.md與r9-to-fantasy-workflow.md。新基準mesh db184294...有240000新取樣列，seed20260913，NEW face/bary namespace，完全不沿用舊模型indices或綁定。後續原圖source外觀/prop仍在隔離製作，最終schema/count未定。真正需保護的是新候選核准外觀、原圖source身份/大小/一次打光、固定點及表面遮擋、相機/章節連續，不是舊退休資料本身。新模型沒有第二組舊手輪廓，勿為復用歷史attr7而導入舊輪廓。主站仍需正式assets、既有章節/keyboard/reducedmotion/fallback/效能及實際UI驗證。此為Coordinator唯讀準備，非main整合核准或任務完成。
