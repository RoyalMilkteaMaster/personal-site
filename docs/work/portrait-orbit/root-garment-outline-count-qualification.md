# Garment r1 reviewer count qualification — settled by exact set definitions

Root independent NumPy read-only check: local-geometry.npz has2148 guarded position IDs,2147 actually differ after float32;3992 faces touch guarded set,3990 touch actual changed positions. The extra two faces are expected conservative support from the one quantized-unchanged guard row. FS1 overlap1717 is intersection of position+normal unions on BOTH sides;1534 is actual position-only intersection with FS1 positionIds. protections.py explicitly computes the union. Neither is a conflicting product result. root-garment-outline-count-qualification.json preserves output. No frozen product edits/new audit required.

Boot centroid vs bary locations are intentionally different surface points, both below native frame; no exact y dispute or body-end interpretation. Future merge must use actual exported field guards/IDs, not a prose count.
