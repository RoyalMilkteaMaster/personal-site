# Original STAR reference projection — coordinator read-only source notes

Primary accepted references are raw-integrated-r2/shots/baseline-front.png (ab2a0644...) and baseline-wide.png (c90f19bc...), both1280x720. Their actual source pipeline is baseline-reference.html/js → original-plate.png leftthird → source-lib/portrait-points.js + source-lib/portrait-intro.js + source-lib/baseline-shaders.js. front-input coloredimage is anatomy support, not automatically samepixelcrop.

Read-only facts: portraitPoints sourcecoords X=(u-.5)*1.04,Y=(.5-v)*1.76,Z=-.22*(1-exp(-distance/(width*.045))). Baseline shader screenX=640+X*[3/(3+Z)]*360,screenY=360-Y*[3/(3+Z)]*360 at1280x720/fit1. Near transforms about(.07,.50,-.14),zoom2.7, pitch-.015,yaw0; XY zoom2.7 butZ scaled sqrt(2.7), then shader depth is recalculated from transformedZ. Thus a handpicked widepixel *2.7 is NOT exact sourcecorrespondence; F39 proveswrongderivednear picks. For futurejointface/nearwidefit use actualoriginalSTARpixels and, if landmarkambiguous, trace the corresponding originalplatepixel throughthispipeline ratherthanpick another chin location and treatit asgroundtruth. This is diagnostics only: never replacecompleteGLBwitholdrelief orusecard/overlayinproduct.

Open ambiguity: oldcamera manualwidechin y215, newheadmanualwidechin244, oldnearDirectchin500; none alone is anatomyproof. Originalface neckbothdark; truejaw endpoint canbe confusedwithneck orlowerhair. Resolveactualreferencecorrespondence beforemorefacegeometry changes. User specificallyrejectsrawreset andrequirescurrentmodelmicrorefinement. CurrentcameraAstraF39fixalreadylocallyreviewednearheadroom butnotfullA13facialregistration. These notesdoNOTauthorizemodelreset ornewglobalshaderrouting.

## Coordinator diagnostic hypothesis for next calibration (not a product conclusion)
For original pitch=0 only, source near transform Xn=s*(X-cx), Yn=s*(Y-cy), Zn=sqrt(s)*(Z-cz), then perspective 3/(3+Zn), is algebraically equivalent to screen multiplier (3*sqrt(s))/(3/sqrt(s)+Z-cz) applied to (X-cx,Y-cy). s=2.7, center(.07,.50,-.14). Thus source near/wide imply a changed optical eye/centre, not merely same low eye with FOV zoom. Actual -.015 pitch must still be included in exact fit. This is an analytical check, not permission to replace mesh with relief.
A camera fit on headfront points already constructed to match the OLD near camera makes old near reprojection exact by construction; that alone cannot validate whether its physical viewing direction is correct for original facial anatomy. If current camera-only fit again fails, inspect actual mesh anatomical view directions and source camera equivalence, possibly jointly recalibrate eye/target and rebuild fixed source triangle binding OFFLINE. Do not assume arbitrary current fixed-origin/el/FOV is user law, and do not replace point positions per runtime angle. Need independent reference landmarks and wholebody/backside constraints; not a claim existing camera is proven wrong.
Follow-up after reading source-lib/portrait-intro.js lines41–48: rotation R is BEFORE anisotropic output scale, so the optical equivalence can actually include exact pitch/yaw: let q=R*(P-C). Screen uses 3*s*qXY/(3+sqrt(s)*qZ) = 3*sqrt(s)*qXY/(3/sqrt(s)+qZ). Equivalent camera in source coordinate convention has orientation R, eye C + R^T*(0,0,-3/sqrt(s)), effective focal3*sqrt(s). This should be verified numerically on exact original source points and baseline shader fit/aspect. It demonstrates original source near/wide can be compatible with a SINGLE fixed3D source under different cameras; it does not authorize replacing current full3D mesh with original relief. Prior -.015 caveat above is resolved algebraically but runtime verification still needed. Original yaw rx=x*cr+z*sr,rz=-x*sr+z*cr.
