# 26 — 自介分組與語言選取色
## 核准範圍
Language 下拉選單未選取項目文字白色，已選取項目文字奶茶色；hover/focus 保持可辨識且不把未選取文字變成選取色。黑底與現有操作保持。
自介在現有右側構圖分成兩組：上方新增較大的「你好」，下一行「我是 皇家奶茶大師」；下方空一行再接「一位 創作者，」「一名 AI全端工程師，」「一個正在一步步實現理想的 夢想家。」維持既有內容與桌面單行末句，留白為組距不插空白朗讀段。問候沿既有顯字動畫，左鍵完成門檻涵蓋完整內容；返回可重播。英文提供等意問候與分組。
「我追求」改為「我勇於追求」，英文對等調整；沿用25票較快引句與較慢三詞節奏。
## 驗證與邊界
桌面優先，1707與900px檢查分組、不溢出/裁字；390px基本可讀。保留人物、1:2內容頁、相機、星空與其他動畫時序，不加依賴不部署不改正式首頁。更新相關copy/reader驗證，適當型別或lint，不擴大測試矩陣。固定小snapshot，兩位獨立審查，Coordinator實際UI。

## 實作選擇追加紀錄（2026-09-16，Coordinator 補記，不變更上方已核准需求）
- 自介顯字時長由 2.4 秒改為 2.8 秒（`INTRO_REVEAL_MS`）。使用者要求新增問候，沒有指定顯字秒數；「允許適當延長」為 Coordinator 派工文字，並非使用者原話。2.8 秒是 Coordinator 在該範圍內的實作選擇，對應新增「你好」後增加的字數與一個組距停頓。其餘段落維持 25 票節奏。左鍵完成門檻仍由 `reveal >= 1` 決定，自動涵蓋完整內容。
- 語言選單鍵盤／滑鼠高亮的外框採不透明奶茶色 2px（Review B 的非文字對比 Finding）。強度屬實作選擇；票上「hover/focus 保持可辨識且不把未選取文字變成選取色」的需求未變，文字色規則不動。
- 以上兩項僅記錄實作選擇與其理由，最終觀感仍待 owner；Reviewer 定向複驗均已通過。

## 完成交付
26-v2 雙審 PASS，兩項重要 Finding 已關閉，Developer 同意證據與结論。一般網址實際操作通過，最終觀感待使用者。產品與 frozen-v2 一致，行政結案更新文件；未部署。
